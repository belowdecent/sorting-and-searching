#include "array.hpp"

void insertion_sort_partial(Array<u16> arr, int gap, int offset);
void insertion_sort(Array<u16> arr);
void shell_sort(Array<u16> arr);
void selection_sort_reverse(Array<u16> arr);
